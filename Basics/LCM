LCM = (a × b) / GCD

function lcm(a,b){

    function gcd(x,y){

        while(y){

            let temp = y;
            y = x % y;
            x = temp;
        }
        return x;
    }

    return (a*b)/gcd(a,b);
}