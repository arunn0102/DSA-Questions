function countSpaces(str) {

    let count = 0;

    for (let ch of str) {
        if (ch === " ") {
            count++;
        }
    }
    return count;
}

console.log(countSpaces("Hello World Java"));