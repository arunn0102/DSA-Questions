function countConsonants(str) {

    let vowels = "aeiouAEIOU";
    let count = 0;

    for (let ch of str) {

        if (/[a-zA-Z]/.test(ch) && !vowels.includes(ch)) {
            count++;
        }
    }
    return count;
}

console.log(countConsonants("Programming"));